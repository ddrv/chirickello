{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "user.added.v1",
  "description": "json schema for task assigned event (version 1)",
  "type": "object",
  "properties": {
    "event": {
      "type": "string",
      "enum": ["task.assigned"]
    },
    "version": {
      "type": "integer",
      "enum": [1],
      "default": 1
    },
    "data": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "format": "uuid"
        },
        "amount": {
          "type": "string",
          "format": "uuid"
        },
        "time": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["taskId", "assignedUserId", "time"]
    }
  },
  "required": ["event", "data"]
}
